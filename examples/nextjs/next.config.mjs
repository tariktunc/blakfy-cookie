/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // The widget script is loaded from jsDelivr inside <BlakfyCookieProvider>,
  // no special config needed here.
};

export default nextConfig;
